import React from "react";
import { View, StyleSheet, TouchableHighlight } from 'react-native'

const CardScreen = ({ onPress, children, bigShadow }) => {
  return(
    <TouchableHighlight>
      
    </TouchableHighlight>
  )
}